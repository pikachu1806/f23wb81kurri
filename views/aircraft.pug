extends layout

block content
  h1= title
  .container-fluid
    .row
      - for (let aircraft of results)
        .col-sm-2
          h2= aircraft.aircraft_model
          p Aircraft Type: #{aircraft.aircraft_type}
          p Aircraft Model: #{aircraft.aircraft_model}
          p Manufacture Year: #{aircraft.manufacture_year}
